#include <stdio.h>

int a[1001], b[1001];

int main ()
{
    int n, k;
    scanf ("%d %d", &n, &k);
    for (int i = 0; i < n; i++)
        scanf ("%d", &a[i]);
    for (int i = 0; i < n; i++)
    return 0;
}
